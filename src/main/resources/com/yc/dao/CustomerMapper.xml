<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 对应接口的全路径 -->
<mapper namespace="com.yc.dao.CustomerMapper">
	<!-- public Customer login(Customer customer); -->
	<select id="login" resultType="Customer">
		select cno,cname,pwd,tel,addr,cphoto,ano,csex,ccodetype,ccodenumber,addition01 from customer where (cname=#{cname} or tel=#{cname}) and pwd=#{pwd}
	</select>
</mapper>